# Dependencies
node_modules/

# Combined web bundle — regenerated by scripts/build-web.mjs
www/

# Capacitor copies the web build + generated config into the native project on
# `cap sync`; all regenerated, so they aren't source.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# Local SDK location (machine-specific)
android/local.properties

# Gradle / build outputs
.gradle/
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/

.DS_Store
